#include <stdio.h>

int main(){
    int a, b;
    scanf("%d %d",&a, &b);
    if (a>b) 
        printf("%d is greater\n", a);
        printf("%d is greater\n", a);
    if (a<b) 
        printf("%d is greater\n", b);
    if (a==b) 
        printf("%d is equal\n", a);
    return 0;
}